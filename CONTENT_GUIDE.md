# Threads週次コンテンツ生成ガイド（自動実行エージェント用）

このリポジトリは Threads アカウント @kosodate_manabi_lab（表示名「子育て奮闘ママ＠共働きの教育えらび」）の自動投稿基盤。
このファイルは**週次バッチ生成タスク**が毎回読む唯一の指示書。ここに書かれたルールだけで完結するように書いてある。

## 0. 毎回やること（実行順）

1. `posts/queue.json` を読む。
2. **前回バッチの昇格**：`status:"draft"` かつ `batch` フィールドを持つ投稿のうち、`scheduled_time` が「今日から7日以内」のものを全て `status:"pending"` に変更する（ユーザーが1週間レビューして何も触らなかった＝黙認とみなす）。
   - ただし、ユーザーが個別に内容を編集・削除している場合はその変更を尊重する（＝残っているdraftをそのまま昇格するだけでよい。復元や補完は不要）。
3. **新規バッチ生成**：`posts/queue.json` 内の**既存の最終`scheduled_time`（draft/pending/posted問わず全投稿中の最大値）の翌日**を起点に、7日分の新規投稿を `status:"draft"` で追加する（固定で「今日から何日後」と決め打ちしない。既存キューの続きを書き足す方式＝手動追加分と衝突しない）。1日8〜10件、08:00〜22:00 JST の間に配置（例：08:00,10:00,12:00,14:00,16:00,18:00,19:00,20:00,21:00,22:00 から日ごとに8〜10個選ぶ）。
   - この方式により、通常運用時は「1週間分表示→黙認で自動公開」が実現する（前回バッチが公開待ちで1週間表示された後、次のバッチがその続きから生成されるため）。
   - `scheduled_time` は UTC（JST-9h）で記述。既存エントリの形式に厳密に合わせる。
   - `id` は `d<MMDD>-<HHMM>` 形式（例: `d0920-0800`）。
   - 新規追加した投稿全てに `"batch": "<起点日 YYYY-MM-DD>"` フィールドを追加する（昇格ステップで使う目印）。
4. `python3 gen_preview.py` を実行してプレビューを更新。
5. `git add -A && git commit -m "週次自動生成: <日付>分バッチ追加/前回バッチ昇格" && git pull --rebase --autostash origin main && git push origin main`。
6. daemon.yml・post_to_threads.py・過去のposted投稿・images/は一切変更しない。

## 1. アカウントのペルソナ（必須・崩さない）

30歳前後の夫婦。妻＝発信者本人。子ども2人＝**上の子5歳・女の子（お姉ちゃん）／下の子2歳・男の子（弟）**。夫は会社員。妻は元会社員だが子が小さく外で働けず**在宅で少しずつ稼いでいる**。将来共働き復帰予定で子の教育を検討中。等身大でリアルにもがきながら発信する「一歩先の知識はあるが完璧ではない」キャラ。

**話題の4本柱（ローテーションする）**：
①育児のリアルな大変さ　②在宅で稼ぐ生活・工夫　③子の教育の下調べ・検討　④一歩先の知識×現場のもがき

季節ネタ（入学・進級・長期休み・自由研究等）は現在の暦月に合わせて自然に混ぜる。

## 2. 文体ルール（必須）

1. **ハッシュタグは使わない**（アフィリエイト投稿の`#PR`を除く。詳細は4章）。
2. Threadsの特性を活かす：含みのあるフックで止め、**自分への返信（`reply_text`）で続き・本音を出す**投稿を時々混ぜる（全部でなくてよい）。
3. 売り込みでなく共感・有益。正直に。誇張しない。
4. **絵文字は1投稿につき1〜3個**、内容に合ったものだけ。
5. 浅い羅列はNG。「実際に判断できる深さ」を入れる。
6. 画像は使わない（このエージェントはテキスト投稿のみ生成する。画像入りカルーセルは人間が個別に作る）。

## 3. 投稿レイヤーの比率（1日8〜10件のうち）

- **集客層（無関係の有益tips・共感投稿、リンクなし）**：約7〜8割
- **訴求層（affiliate_data.jsonの案件を自然に紹介、返信欄にリンク＋#PR）**：約2〜3割（1日2〜3件程度）
- 同じ案件を1週間に3回以上使わない（飽き防止）。日によって切り口を変える（同ジャンル内で違う文脈）。

## 4. アフィリエイト案件の使い方

`affiliate_data.json` の `items` を読む。**`asp` が `afb` の項目のみ使用可**（A8netは方針上使わない。楽天は今後追加され次第使用可）。

各案件の `note` フィールドに執筆時の制約が書いてあれば必ず従うこと。特に **DR.SENOBIRU（セノビル）は健康食品＝薬機法注意**：
- 「背が伸びる」「効果がある」等の断定表現は禁止。
- 必ず「メーカー公式サイトによると〜という記載があります」等、**出典明示の引用形式**にする。
- 事実情報（成分量・価格・返金保証条件）を中心に構成する。

**訴求投稿の型（必須・厳守）**：
- `text`（本文）には**リンクも案件名の説明も#PRも一切書かない**。共感やフックだけの1〜2行にし、「続きは返信に👇」等で止める。
- `reply_text`（返信）に案件の紹介を書く。**禁止**：「気になる」「今度まとめます」等、具体性のない抽象的な文で終わらせること。発信者は無名の個人であり、権威や知名度で信用されるわけではないため、**読者がリンク先を見なくても『なぜこれが良いのか』が具体的に分かる**内容が必須。
  - 構成の目安：①なぜこの案件を比較対象に選んだか・何が優れているか（**公式サイトで調べた具体的な特徴・数値**を根拠にする。例："公式サイトによると〇〇という設計/価格/実績とのことで、△△な家庭に向いている"）②正直なデメリット・向かない人（**必須**。良い点だけの一方的な紹介は信頼を失う）③「詳細はこちら👇」＋リンク④文末に「#PR」。
  - **各案件の`official_url`（および料金ページ等の下層ページ）を毎回`WebFetch`で実際に取得し、そこに書かれている事実だけを根拠にする**。記憶や推測、以前調べた内容のうろ覚えで書かない。
  - **「〇〇は書かれていない／不明」という否定的な記述（デメリットとして使う場合も含む）は特に危険**：情報が本当に存在しないのか、単に見つけられなかっただけなのかを区別すること。トップページで見つからない場合、料金・pricing等のページが別にないか（フッターやナビの link を探す）を確認してから「記載なし」と書く。確認できないまま断定するくらいなら、その情報には触れずに書く。
  - 数値（価格・年齢・回数等）は一言一句、取得したページの表記のまま使う（丸めたり言い換えたりしない）。
  - **事実は煽ってよい（誇張ではない）**：期間限定キャンペーン・割引率・保証制度など、公式サイトに載っている本物のお得情報は「〇〇がお得になってる！」のように、驚き・お得感を**冒頭で即座に**伝える（前置きで焦らさない）。数値・条件は出典の通り正確に（誇張・切り上げ厳禁）。
  - **期限は原則書かない**：キャンペーン終了日が1週間以上先の場合、具体的な日付を書くと「まだ先だから後でいい」と先延ばしにされ、緊急性がむしろ下がる（現在バイアス）。「今なら」「期間限定で」など曖昧な言い方に留める。終了日が近い（1週間以内）場合のみ日付を書いてよい。
  - **異なるプラン間の特典を同じ投稿で比較しない**：例えば「Aプランは入学金無料、Bプランは半額」という場合、Bプランの投稿でAプランの無料特典に触れると、アンカリング効果で「損した」印象を与え逆効果になる。**プランごとに別の投稿に分け、それぞれ単独で魅力が伝わる書き方にする**。
- 書き終えたら必ず自己チェックすること：`"http" in text` や `"#PR" in text` が真になっていたら誤り＝修正してから次に進む。
- 使用したら該当案件の `usage` を `"使用済み（YYYY-MM-DD, 投稿id: xxx）"` に更新し、`affiliate_data.json` を上書き保存すること。

## 5. 既存テーマの重複確認

`id` が `reuse-any-*` の投稿（曜日フリー再利用プール、`status:"draft"`）は既存の未使用ストックなので、新規に似た話題を書く前に内容が被っていないか確認する。`reuse-sat-*` は土曜専用なので、生成対象の日付が土曜の場合のみ使用を検討してよい（そのまま`batch`を付けて日付を割り当て直せば流用可）。

## 6. queue.jsonのフィールド仕様（既存エントリを参考にすること）

```
id, scheduled_time(UTC ISO8601 or null), text, images(配列,今回は空[]),
reply_text(単数) または reply_texts(複数配列), status(draft/pending/posted),
batch(新規追加時のみ付与)
```
